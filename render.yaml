services:
  - type: web
    name: ytdl-server
    env: python
    plan: free
    branch: main
    buildCommand: |
      pip install -r requirements.txt
      chmod +x /opt/render/project/src/yt-dlp
    startCommand: gunicorn server:app
    autoDeploy: true
